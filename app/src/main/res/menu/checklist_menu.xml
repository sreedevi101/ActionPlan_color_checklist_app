<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_share"
        android:icon="@drawable/baseline_share_24"
        android:title="@string/share_list"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_rearrange_list"
        android:icon="@drawable/baseline_drag_handle_24"
        android:title="@string/rearrange_tasks"
        app:showAsAction="ifRoom" />


    <group
        android:id="@+id/deleting">

        <!-- "Delete all", should appear as action button if possible -->
        <item
            android:id="@+id/action_delete_all_tasks"
            android:title="@string/delete_all_tasks"
            app:showAsAction="never" />

    </group>


    <group
        android:id="@+id/formatting">

        <item
            android:id="@+id/action_change_background"
            android:title="@string/change_background"
            app:showAsAction="never" />

        <item
            android:id="@+id/action_format_all_items"
            android:title="@string/style_list_items"
            app:showAsAction="never" />

    </group>

    <!-- [DEBUG] Print the database tables in the Log -->
    <item
        android:id="@+id/action_print_database"
        android:title="[DEBUG] Print Database"
        app:showAsAction="never"
        tools:ignore="HardcodedText" />

</menu>